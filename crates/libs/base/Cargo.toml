[package]
name = "lib-base"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = "0.21"
time = {version = "0.3", features = ["formatting", "parsing"]}
uuid = {version = "1", features = ["v4","fast-rng",]}
